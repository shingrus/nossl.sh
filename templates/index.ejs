<!DOCTYPE html>
<html lang="en">
  <head>
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "nossl.sh",
        "applicationCategory": "DeveloperApplication",
        "operatingSystem": "Windows, macOS, Linux",
        "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
        "url": "https://nossl.sh/",
        "publisher": {
          "@type": "Organization",
          "name": "nossl.sh",
          "url": "https://nossl.sh/"
        }
      }
    </script>
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "Why won't the Wi-Fi login page load?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Captive portals sometimes block HTTPS handshakes or VPN/DNS profiles. Open http://nossl.sh in your browser, turn off VPNs, and retry so the network can show its sign-in splash screen."
            }
          },
          {
            "@type": "Question",
            "name": "What is the URL to force Wi-Fi login?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Visit http://nossl.sh to trigger most captive portal login pages because it intentionally serves plain HTTP without HSTS."
            }
          },
          {
            "@type": "Question",
            "name": "How do I connect to the Wi-Fi login page?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Turn off VPNs, then visit http://nossl.sh in a browser to trigger the captive portal. If it does not appear, reload, try a private window, or toggle Wi-Fi off/on."
            }
          }
        ]
      }
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="nossl.sh helps you diagnose no SSL connection issues, check your Wi-Fi, verify internet access, troubleshoot captive portal login pages, and inspect the headers your device sends."
    />
    <meta
      name="keywords"
      content="no ssl, nossl, never ssl alternative, captive portal login, hotel wifi login, airport wifi, coffee shop wifi, http test page"
    />
    <meta property="og:title" content="nossl.sh - Force Wi-Fi Login, Check IP & Headers (HTTP Only)" />
    <meta
      property="og:description"
      content="nossl.sh helps you diagnose no SSL connection issues, check your Wi-Fi, verify internet access, troubleshoot captive portal login pages, and inspect the headers your device sends."
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="http://nossl.sh/" />
    <meta property="og:site_name" content="nossl.sh" />
    <meta property="og:image" content="/static/og-image.png" />
    <meta property="og:image:alt" content="Abstract teal and navy banner for nossl.sh" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="nossl.sh - Force Wi-Fi Login, Check IP & Headers (HTTP Only)" />
    <meta
      name="twitter:description"
      content="nossl.sh helps you diagnose no SSL connection issues, check your Wi-Fi, verify internet access, troubleshoot captive portal login pages, and inspect the headers your device sends."
    />
    <meta name="twitter:image" content="/static/og-image.png" />
    <meta name="twitter:image:alt" content="Abstract teal and navy banner for nossl.sh" />
    <title>nossl.sh - Get Wi-Fi Login & Check Your IP</title>
    <link rel="icon" href="/favicon.ico" type="image/svg+xml" />
    <link rel="stylesheet" href="/static/styles.css?v=2" />
  </head>
  <body>
    <% 
      const headerNote = '// Trigger the captive hotel/airport Wi‑Fi login page, never HTTPS, never SSL';
      const footerLines = [
        `Prefer JSON? Try the <a href="/api/request-info" rel="nofollow">request info API endpoint</a>. Total requests served: <span class="mono">${totalRequests.toLocaleString('en-US')}</span>`
      ];
    %>
    <%- include('partials/header', {headerNote}) %>

    <main class="container bento-page">
      <%- include('partials/connection-report') %>

      <section class="bento-section" aria-label="Diagnostics tools">
        <div class="bento-grid bento-grid--secondary">
          <article class="tile tile--span-4 info-card" aria-labelledby="quick-commands">
            <p class="eyebrow">Quick commands</p>
            <h3 id="quick-commands" class="tile__title">cURL shortcuts</h3>
            <p class="muted">Use these copy-ready commands for IPv4, IPv6, and status checks.</p>
            <div class="code-block">
              <code class="mono">$ curl http://nossl.sh</code>
              <button type="button" class="btn btn-outline btn-small" data-copy-text="curl http://nossl.sh">Copy</button>
              <div class="code-example mono">→ <%= clientIp %></div>
            </div>
            <div class="code-block">
              <code class="mono">$ curl http://v6.nossl.sh</code>
              <button type="button" class="btn btn-outline btn-small" data-copy-text="curl http://v6.nossl.sh">Copy</button>
              <div class="code-example mono">→ ipv6</div>
            </div>
            <div class="code-block">
              <code class="mono">$ curl -i http://nossl.sh/status/418</code>
              <button type="button" class="btn btn-outline btn-small" data-copy-text="curl -i http://nossl.sh/status/418">Copy</button>
              <div class="code-example mono">→ HTTP/1.1 418 I'm a Teapot</div>
            </div>
            <div class="code-block">
              <code class="mono">$ curl 'http://nossl.sh/api/ip?1.1.1.1'</code>
              <button type="button" class="btn btn-outline btn-small" data-copy-text="curl 'http://nossl.sh/api/ip?1.1.1.1'">Copy</button>
            </div>
          </article>

          <article class="tile tile--span-4 info-card" aria-labelledby="tools">
            <p class="eyebrow">Tools</p>
            <h3 id="tools" class="tile__title">Captive portal helpers</h3>
            <p class="muted">Jump to focused tools and reports built for Wi‑Fi troubleshooting.</p>
            <div class="tool-grid">
              <a class="tool-card" href="/captive-portal">Captive portal diagnostics</a>
              <a class="tool-card" href="/wifi-login-page">Wi‑Fi login page tester</a>
              <a class="tool-card" href="/mobile-wifi">Mobile Wi‑Fi troubleshooting</a>
              <a class="tool-card" href="/list-of-countries-by-ipv4-allocation">List of countries</a>
              <a class="tool-card" href="/top-asn-by-ip-address">Top ASNs</a>
              <a class="tool-card" href="/free-geo-ip" rel="nofollow">Free Geo + ASN lookup</a>
            </div>
          </article>

          <article class="tile tile--span-4 info-card" aria-labelledby="guide-faq">
            <p class="eyebrow">Guide & FAQ</p>
            <h3 id="guide-faq" class="tile__title">Plain HTTP for quick checks</h3>
            <p class="muted">See your real IP, headers, and captive portal behavior without HTTPS in the way.</p>
            <ul class="bullets">
              <li>Open http://nossl.sh to trigger login pages.</li>
              <li>Share a snapshot link if support needs proof.</li>
              <li>Use the DNS resolver badge to confirm recursion.</li>
            </ul>
            <p class="muted">Browse the <a href="/guides">full guide index</a> for step-by-step help.</p>
            <dl class="faq">
              <dt>Why no TLS?</dt>
              <dd>Captive portals often block HTTPS handshakes. HTTP keeps the splash page visible.</dd>
              <dt>Is there an API?</dt>
              <dd>Yes — use <a href="/api/request-info" rel="nofollow">/api/request-info</a> for JSON.</dd>
            </dl>
          </article>

          <article class="tile tile--span-6 info-card" aria-labelledby="api">
            <p class="eyebrow">API</p>
            <h3 id="api" class="tile__title">Endpoints you can script</h3>
            <p class="muted">Keep these URLs handy for diagnostics, dashboards, and debug scripts.</p>
            <ul class="bullets">
              <li><a href="/api/request-info" rel="nofollow">/api/request-info</a> — current IP, scheme, headers.</li>
              <li><a href="/api/counters" rel="nofollow">/api/counters</a> — request counters snapshot.</li>
              <li><a href="/api/honeypot" rel="nofollow">/api/honeypot</a> — honeypot hits as JSON.</li>
              <li><a href="/api/ip?1.1.1.1" rel="nofollow">/api/ip?1.1.1.1</a> — Geo + ASN for any IP.</li>
              <li><a href="/api/as15169" rel="nofollow">/api/as15169</a> — ASN details, org, and prefixes.</li>
              <li><a href="/status/200" rel="nofollow">/status/:code</a> — return any HTTP status; add <code class="mono">?location=</code> for redirects.</li>
            </ul>
            <p class="muted">Need the honeypot console? Visit the <a href="/honeypot">/honeypot</a> dashboard.</p>
            <p class="muted">Need service counters? Visit the <a href="/ss">service status page</a>.</p>
          </article>

          <article class="tile tile--span-6 info-card" aria-labelledby="faq">
            <p class="eyebrow">FAQ</p>
            <h3 id="faq" class="tile__title">Why plain HTTP?</h3>
            <dl class="faq">
              <dt>Why no TLS?</dt>
              <dd>Captive portals and restrictive networks often block HTTPS handshakes. HTTP keeps the splash page visible.</dd>
              <dt>What can I use this for?</dt>
              <dd>Spot VPN or proxy issues, capture headers for debugging, and confirm whether the network is online.</dd>
              <dt>Can I see my DNS resolver?</dt>
              <dd>Yes — the connection report shows the DNS resolver IP using a beacon lookup.</dd>
              <dt>Do you log anything?</dt>
              <dd>Only what the request already sends. No cookies, accounts, or tracking.</dd>
            </dl>
          </article>

          <%- include('partials/support-block') %>
        </div>
      </section>
    </main>

    <%- include('partials/footer', {generationTimeMs, footerLines, generatedAt}) %>

    <%- include('partials/connection-report-script') %>
  </body>
</html>
