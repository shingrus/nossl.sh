# --- HTTP: serve ALL subdomains over plain HTTP ---
server {
  listen 80;
  server_name  .nossl.sh;   # wildcard matches *.nossl.sh

  proxy_hide_header Strict-Transport-Security;

   set $cors_origin "";

  if ($http_origin ~* ^https?://(nossl\.sh|v4\.nossl\.sh|v6\.nossl\.sh)$) {
    set $cors_origin $http_origin;
  }

     
  # proxy to nossl Node app
     location / {
      if ($request_method = OPTIONS) {
      add_header Access-Control-Allow-Origin $cors_origin;
      add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
      add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With";
      add_header Access-Control-Max-Age 86400;
      add_header Content-Length 0;
      add_header Content-Type text/plain;
      return 204;
     }

    # CORS for normal requests
    add_header Access-Control-Allow-Origin $cors_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;

    
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
  }
}

# --- HTTPS: apex only -> 301 to random HTTP subdomain ---
#server {
#  listen 443 ssl http2;
#  server_name nossl.sh;

#  ssl_certificate     /etc/letsencrypt/live/nossl.sh/fullchain.pem;
#  ssl_certificate_key /etc/letsencrypt/live/nossl.sh/privkey.pem;

  # Random-ish DNS-safe label; use $request_id if available, otherwise $msec
#  set $rand $request_id;  # or: set $rand $request_id;

  # Use 302 while testing to avoid cache, then switch to 301
#  return 302 http://$rand.nossl.sh$request_uri;
  #return 204;
#}


# --- HTTPS: wildcard subdomains -> serve app (like HTTP) ---
#server {
   
#  listen 443 ssl http2;
#  server_name .nossl.sh;

#  ssl_certificate     /etc/letsencrypt/live/nossl.sh/fullchain.pem;
#  ssl_certificate_key /etc/letsencrypt/live/nossl.sh/privkey.pem;

#  proxy_hide_header Strict-Transport-Security;
  
#  location / {
#    proxy_pass http://127.0.0.1:8080;
#    proxy_set_header Host $host;
#    proxy_set_header X-Forwarded-For $remote_addr;
#    proxy_set_header X-Forwarded-Proto https;
#  }
#}
